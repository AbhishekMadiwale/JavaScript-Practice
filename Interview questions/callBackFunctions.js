x(function y() {
    console.log("y")
})

/**
 * The above code is call back function
 * we are calling function y inside function x, this is the structure of the call back function
 * 
 * f.e. setTimeOut(function() { 
 * console.log('inside call back')}, 3000)
 * 
 * the above setTimeOut function is also a call back function
 */

/**
 * Evenet listerners are also example of call back function
 */